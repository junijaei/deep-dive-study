## 10. 객체 리터럴

### 10.3 프로퍼티

- 프로퍼티 키에 문자열/심벌 외의 값을 사용하면 암묵적 타입 변환을 통해 문자열이 된다.
- 식별자 네이밍 규칙을 따르지 않는 키 값의 경우 무조건 따옴표로 감싸야 한다.

### 10.5 프로퍼티 접근

- 마침표 표기법 `object.name` / 대괄호 표기법 `object['name']`
- 객체에 존재하지 않는 프로퍼티에 접근하면 undefined를 반환한다.